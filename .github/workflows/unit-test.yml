name: Run Unit Tests

on:
  pull_request:
    branches: ['**']

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18, 20]

    steps:
      - name: Checkout zlux-server-framework (PR branch)
        uses: actions/checkout@v4
        with:
          path: zlux-server-framework

      - name: Determine target branch
        id: branch
        run: echo "name=${{ github.event.pull_request.base.ref }}" >> "$GITHUB_OUTPUT"

      - name: Checkout zlux-shared
        uses: actions/checkout@v4
        with:
          repository: zowe/zlux-shared
          ref: ${{ steps.branch.outputs.name }}
          path: zlux-shared

      - name: Checkout zlux-platform
        uses: actions/checkout@v4
        with:
          repository: zowe/zlux-platform
          ref: ${{ steps.branch.outputs.name }}
          path: zlux-platform

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Debug directory structure
        run: |
          echo "=== github.workspace ==="
          echo "${{ github.workspace }}"
          echo "=== workspace contents ==="
          ls -la ${{ github.workspace }}/
          echo "=== one level up ==="
          ls -la ${{ github.workspace }}/../
          echo "=== find zlux-shared ==="
          find /home/runner/work -maxdepth 4 -name "zlux-shared" -type d 2>/dev/null
          echo "=== find package.json in zlux-shared ==="
          find /home/runner/work -path "*/zlux-shared/lib/package.json" 2>/dev/null

      - name: Build zlux-shared
        working-directory: ${{ github.workspace }}/zlux-shared/lib
        run: |
          npm install
          npm run build

      - name: Build zlux-platform
        working-directory: ${{ github.workspace }}/zlux-platform/interface
        run: |
          npm install
          npm run build

      - name: Install zlux-server-framework dependencies
        working-directory: ${{ github.workspace }}/zlux-server-framework
        run: npm install

      - name: Build zlux-server-framework
        working-directory: ${{ github.workspace }}/zlux-server-framework
        run: npm run build

      - name: Run tests with coverage
        working-directory: ${{ github.workspace }}/zlux-server-framework
        run: npm run test:coverage

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-node-${{ matrix.node-version }}
          path: ${{ github.workspace }}/zlux-server-framework/coverage/
          retention-days: 30